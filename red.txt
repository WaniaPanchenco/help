sudo wget https://archive.kali.org/archive-keyring.gpg -O /usr/share/keyrings/kali-archive-keyring.gpg
или
curl -fsSL https://archive.kali.org/archive-keyring.gpg | sudo tee /usr/share/keyrings/kali-archive-keyring.gpg > /dev/null

sudo apt update
file unknown_file
strings unknown_file | grep -iE "flag|key|ctf|password|secret"
binwalk -e unknown_file
exiftool unknown_file

pngcheck -v 
‎
‎xxd файл | grep -A5 -By -i "flag\|ctf"
‎
‎pngseek
‎

zsteg -a PNG
exiftool image.jpg | grep -i flag
gdb ./file

Открыл бинарник -> Search -> For Strings -> Filter: "flag".

Shift+F12 (Ida Pro)

sudo apt install openjdk-17-jdk -y
wget https://github.com/NationalSecurityAgency/ghidra/releases/download/Ghidra_11.2.1_build/ghidra_11.2.1_PUBLIC_20241105.zip

sudo unzip ghidra_11.2.1_PUBLIC_*.zip -d /opt
sudo ln -s /opt/ghidra_11.2.1_PUBLIC/ghidraRun /usr/local/bin/ghidra


vlan.id==60
stp.flags.tp==1 (первый stp)
tcp.flags.syn==1

sudo apt update && sudo apt install qemu-utils
qemu-img convert -f vmdk -O qcow2 /path/to/mydisk.vmdk /path/to/mydisk.qcow2


rd.break enforcing=0
‎mount -o remount,rw /sysroot
‎chroot /sysroot
‎passwd root
‎touch /.autorelabel
‎exit
‎exit
‎reboot
‎
‎ip addr add 192.168.20.21/24 dev ens33
‎ip route add default via 192.168.20.1
‎
‎systemctl restart tomcat
‎systemctl stop firewalld
‎
