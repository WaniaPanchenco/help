mkdir /srv/data/
chown 36:36 /srv/data/
chmod 0755 /srv/data/
engine-setup —accept-defaults —offline
engine-config -s ImageTransferProxyEnabled=false
systemctl restart ovirt-engine 
Сеть - vNIC(как то так) - нет сетевого фильтра
Виртуализация - Дата центры - выбрать нужный и нажать изменить - тип хранилища локальный
Виртуализация - Узлы - новый (если что в вкладке кластеры можно менять процессоры)
Хранилище - Домены - Новый домен - тип хранилища "локально на хосте", имя "data", путь /srv/data
Хранилище - Диски - Загрузить - запуск (загрузка образов)
Дальше идем в портал виртуальных машин


scp /home/kali/Desktop/disc1.vmdk user@192.168.20.10:/home/user/dis (папку dis создаем)

Идем создаем кластер
Не забываем создать пустую виртуалку, потом идем в host2 - сеть и создаем бридж (Имя vmbr0, CIDR 192.168.30.50/24 Шлюз 192.168.30.1)    Если ошибка - то перезагрузить apt-get

перед format всегда идет два минуса

qm importdisk 100 /home/alt/disk.vmdk local —format vmdk (local это имя стораджа, 100 это идентификатор машины)
Дальше идем в порядок загрузки


qemu-img convert -f vmdk -O qcow2 file.vmdk filenew.qcow2

sudo apt update
sudo apt install qemu-utils

sudo dnf install libvirt qemu-kvm virt-install virt-manager openssh-askpass OVMF boost-random boost-program-options boost-regex bridge-utils


В кордах при создании даем имя XFA и выбираем "использует другой координатор в качестве VPN сервера"
Клиенты - создаем админа и двух клиентов, первому первый корд, второму второй
Идем в корды, выдаем роли, затем в межсерверные каналы и выбираем второй корд 
(аналогично делаем и с другим, фаерволам 
только роли узла)
Далее идем в клиенты - администратор - роли, юзера только client for linux
Пользователи - администратор - связи с получателями - всех
Корды - связи с пользователями - своего XFA и другого корда
Справочники и ключи - создать справочники - для всего списка
Далее ставим УКЦ
УКЦ - сетевые узлы - выделяем все - выдать новый дистрибутив ключей
УКЦ - выделяем всех - создать и передать ключи в цус
Пользователи - ключи пользователя - создать и передать в цус
Дальше ставим випнет клиент, который в начале папки софт випнет и перезагружаемся


